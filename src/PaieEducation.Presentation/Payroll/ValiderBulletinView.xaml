<UserControl x:Class="PaieEducation.Presentation.Payroll.ValiderBulletinView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="500">
    <StackPanel Margin="24" MaxWidth="420" HorizontalAlignment="Left">
        <TextBlock Text="Valider un bulletin" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <TextBlock Text="Identifiant agent"/>
        <TextBox Text="{Binding AgentId, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <TextBlock Text="Date de paie (AAAA-MM-JJ)"/>
        <TextBox Text="{Binding DatePaie, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

        <Button Content="Valider" Command="{Binding ValiderCommand}"
                Width="120" HorizontalAlignment="Left" Margin="0,8,0,8"/>

        <TextBlock Text="Validation en cours…" Foreground="Gray" Margin="0,0,0,8">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding EnCours}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <TextBlock Text="{Binding Resultat}" TextWrapping="Wrap" FontWeight="SemiBold"/>
    </StackPanel>
</UserControl>
