using System.Windows.Controls;

namespace PaieEducation.Presentation.Payroll;

/// <summary>Code-behind minimal — le <c>DataContext</c> est posé par le <c>DataTemplate</c> implicite (aucune logique ici, MVVM strict).</summary>
public partial class CalculerBulletinView : UserControl
{
    public CalculerBulletinView() => InitializeComponent();
}
