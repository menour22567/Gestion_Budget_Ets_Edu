namespace PaieEducation.Domain.Workbench.ValueObjects;

/// <summary>Projection en lecture d'une <c>Rubrique</c> — pour la matrice de couverture (D11, J3I §5.5).</summary>
public sealed record RubriqueResume(string Id, string Libelle);
