<UserControl x:Class="PaieEducation.Presentation.Workbench.MatriceCouvertureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="900">
    <StackPanel Margin="24" HorizontalAlignment="Left">
        <TextBlock Text="Matrice de couverture" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="Date de paie (AAAA-MM-JJ)"/>
                <TextBox Text="{Binding DatePaie, UpdateSourceTrigger=PropertyChanged}" Width="140"/>
            </StackPanel>
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="Filtre corps"/>
                <TextBox Text="{Binding FiltreCorps, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
            </StackPanel>
            <StackPanel Margin="0,0,16,0">
                <TextBlock Text="Filtre rubrique"/>
                <TextBox Text="{Binding FiltreRubrique, UpdateSourceTrigger=PropertyChanged}" Width="160"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Filtre état"/>
                <ComboBox ItemsSource="{Binding EtatsFiltrables}" SelectedItem="{Binding FiltreEtat}" Width="160"/>
            </StackPanel>
        </StackPanel>

        <Button Content="Charger" Command="{Binding ChargerCommand}"
                Width="120" HorizontalAlignment="Left" Margin="0,0,0,8"/>

        <TextBlock Text="Chargement en cours…" Foreground="Gray" Margin="0,0,0,8">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding EnCours}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <TextBlock Margin="0,0,0,8" FontStyle="Italic" Foreground="Gray"
                   Text="Légende : ✓ vert = couverte active · ~ orange = couverte inactive (expirée/future) · — rouge = non couverte. Clic sur une cellule → fiche de la rubrique."/>

        <DataGrid x:Name="Grille" ItemsSource="{Binding Lignes}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column"
                  HorizontalScrollBarVisibility="Auto" MaxHeight="480"/>
    </StackPanel>
</UserControl>
