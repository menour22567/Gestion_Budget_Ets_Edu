<Window x:Class="PaieEducation.Presentation.Shell.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="PaieEducation" Height="600" Width="960">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Paie">
                <MenuItem Header="_Calculer un bulletin" Command="{Binding NaviguerVersCalculerBulletinCommand}"/>
                <MenuItem Header="_Valider un bulletin" Command="{Binding NaviguerVersValiderBulletinCommand}"/>
                <MenuItem Header="C_onsulter un bulletin" Command="{Binding NaviguerVersConsulterBulletinCommand}"/>
            </MenuItem>
            <MenuItem Header="_Agents">
                <MenuItem Header="_Liste des agents" Command="{Binding NaviguerVersListeAgentsCommand}"/>
                <MenuItem Header="_Créer un agent" Command="{Binding NaviguerVersCreerAgentCommand}"/>
                <MenuItem Header="_Suggérer des rubriques" Command="{Binding NaviguerVersSuggererRubriquesCommand}"/>
            </MenuItem>
            <MenuItem Header="_Référentiels">
                <MenuItem Header="_Grille indiciaire" Command="{Binding NaviguerVersGrilleIndiciaireCommand}"/>
            </MenuItem>
            <MenuItem Header="_Workbench réglementaire">
                <MenuItem Header="_Vue d'ensemble" Command="{Binding NaviguerVersWorkbenchCommand}"/>
                <MenuItem Header="_Matrice de couverture" Command="{Binding NaviguerVersMatriceCouvertureCommand}"/>
                <MenuItem Header="_Audit &amp; traçabilité" Command="{Binding NaviguerVersAuditLogCommand}"/>
                <MenuItem Header="_Fiche rubrique" Command="{Binding NaviguerVersFicheRubriqueCommand}"/>
                <MenuItem Header="_Éditer une rubrique" Command="{Binding NaviguerVersEditerRubriqueCommand}"/>
            </MenuItem>
        </Menu>
        <TabControl ItemsSource="{Binding Onglets}" SelectedItem="{Binding OngletActif}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Titre}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <Button Content="✕" Padding="4,0" BorderThickness="0" Background="Transparent"
                                Command="{Binding FermerCommand}"
                                Visibility="{Binding EstFermable, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentControl Content="{Binding Contenu}"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </DockPanel>
</Window>
