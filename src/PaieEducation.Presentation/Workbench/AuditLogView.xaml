<UserControl x:Class="PaieEducation.Presentation.Workbench.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="900">
    <DockPanel Margin="24">
        <TextBlock Text="Audit &amp; traçabilité" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" DockPanel.Dock="Top"/>

        <WrapPanel Margin="0,0,0,8" DockPanel.Dock="Top">
            <TextBlock Text="Acteur :" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Text="{Binding FiltreActeur, UpdateSourceTrigger=PropertyChanged}" Width="110" Margin="0,0,12,0"/>

            <TextBlock Text="Action :" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Text="{Binding FiltreAction, UpdateSourceTrigger=PropertyChanged}" Width="160" Margin="0,0,12,0"/>

            <TextBlock Text="Type d'entité :" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Text="{Binding FiltreTypeEntite, UpdateSourceTrigger=PropertyChanged}" Width="110" Margin="0,0,12,0"/>

            <TextBlock Text="Du (aaaa-mm-jj) :" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Text="{Binding FiltreDateDebut, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="0,0,12,0"/>

            <TextBlock Text="Au (aaaa-mm-jj) :" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Text="{Binding FiltreDateFin, UpdateSourceTrigger=PropertyChanged}" Width="100" Margin="0,0,12,0"/>

            <Button Content="Filtrer" Command="{Binding ChargerCommand}" Width="100" Margin="0,0,8,0"/>
            <TextBlock Text="Chargement en cours…" Foreground="Gray" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding EnCours}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </WrapPanel>

        <Button Content="Charger plus" Command="{Binding ChargerPlusCommand}" Width="120"
                HorizontalAlignment="Left" Margin="0,8,0,0" DockPanel.Dock="Bottom"/>

        <DataGrid ItemsSource="{Binding Entrees}" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Horodatage (UTC)" Binding="{Binding OccurredAt}" Width="180"/>
                <DataGridTextColumn Header="Acteur" Binding="{Binding Actor}" Width="90"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="200"/>
                <DataGridTextColumn Header="Type d'entité" Binding="{Binding EntityType}" Width="110"/>
                <DataGridTextColumn Header="Id" Binding="{Binding EntityId}" Width="130"/>
                <DataGridTextColumn Header="Commentaire" Binding="{Binding Comment}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
